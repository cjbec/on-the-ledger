{
  "name": "on-the-ledger",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env scheduler.js",
    "pipeline": "node --env-file=.env scheduler.js --run full",
    "rss": "node --env-file=.env scheduler.js --run rss",
    "edgar": "node --env-file=.env scheduler.js --run edgar",
    "rollup": "node --env-file=.env scheduler.js --run rollup",
    "dev": "node --env-file=.env server.js & vite",
    "dev:api": "node --env-file=.env server.js",
    "build": "vite build"
  },
  "dependencies": {
    "node-cron": "^3.0.0",
    "fast-xml-parser": "^4.0.0",
    "@vercel/blob": "^0.15.0",
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  }
}
